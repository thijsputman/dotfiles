# ToDo

- [ ] Find better way to set Powerline defaults in `.bashrc` (i.e. don't set
      them in `.bashrc` but instead source a file that isn't version controlled
      to allow for easy customisation between machines)
